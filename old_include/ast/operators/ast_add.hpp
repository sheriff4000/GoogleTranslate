class add : public node
{
public:
    //how to evaluate the branches
    //will the gen mips function return the right values?
    //addition is a register instruction - so we nee values in registers
    //should be specify which registers in this class, so the branches will input into the registers, 
    //or do we let the, e.g. int literal + variable classes decide which registers, then return them up to the operator
    gen_mips()
};